name: keep-alive supabase
on:
  schedule:
    - cron: 0 13 * * *
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping a Supabase
        run: |
          curl -s "https://lknvospoabzkyljelcuy.supabase.co/rest/v1/app_config?select=key&limit=1" -H "apikey: sb_publishable_m1SllmJ_TksvKl8pRGA5RA_BlUjvE7S"
